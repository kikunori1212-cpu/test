# プロジェクト概要

このプロジェクトは Claude Code をメインインターフェースとして業務を進めるための作業環境です。
資料作成・コーディング・タスク管理などを Claude Code とのチャットを通じて行います。

---

## ディレクトリ構成

```
testpj/
├── CLAUDE.md        # このファイル（Claude Code が毎回読み込む）
├── docs/            # 資料・ドキュメント・仕様書
├── src/             # コード・スクリプト
├── tasks/           # タスク・TODO・進捗管理
└── tmp/             # 一時ファイル（Git 管理外）
```

---

## セッション開始時の手順

新しい会話が始まったら、必ず最初に以下を行う：

1. `tasks/current.md` を読み込んで前回の状況を把握する
2. 「前回は〇〇をしていました。続きからですか？」と確認する
3. セッション終了時（ユーザーが終わりを告げたとき）は `tasks/current.md` を最新状態に更新する

---

## Claude への指示

### 基本方針
- 回答は**日本語**で行う
- 簡潔・明確に答える。不要な前置きや要約は省く
- 提案するときは選択肢を絞って具体的に示す

### 資料作成
- Markdown 形式を基本とする
- ファイルは `docs/` に保存する
- ファイル名は内容がわかる日本語または英語で命名する

### コーディング
- コードは `src/` に保存する
- 言語・フレームワークはタスクごとに指定する
- セキュリティ上のリスクがある実装は必ず警告する

### タスク管理
- 進行中の作業は `tasks/` に Markdown で記録する
- タスクのステータスは TODO / IN PROGRESS / DONE で管理する

---

## よく使うコマンド

```bash
# ファイル一覧確認
ls -la

# Git 状態確認
git status

# コミット（明示的に依頼があった場合のみ実行）
git add <files> && git commit -m "メッセージ"
```
